import { handleAmounts } from './amounts-actions';
import {handleCurrencies, fetchCurrencies} from "./currencies-actions";
import {setRates} from "./rates-actions";

export {
    handleAmounts,
    handleCurrencies,
    fetchCurrencies,
    setRates
}
